# ~/.config/containers/systemd/mbus_to_usb.env

# Environment file for mbus_to_usb service
# Copy this file to ~/.config/containers/systemd/mbus_to_usb.env and adjust the values as needed
# pay attention NOT to escape special characters like $ in MQTT_PASSWD etc.: they are taken verbatim and authn might fail otherwise

# Serial key for smart meter access
SERIAL_KEY=your_serial_key_here

# MQTT broker configuration
MQTT_SERVER=localhost
MQTT_PORT=1883
MQTT_USER=your_mqtt_user
MQTT_PASSWD=your_mqtt_password

# Optional: Sentry error tracking URL
# SENTRY_URL=https://your_sentry_url_here
